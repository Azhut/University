##  раткое описание:

Ётот код демонстрирует использование линейного аллокатора в контейнере `std::map` и измерение времени выполнени€ операций.

### ‘ункции и классы:

- `CStringComparator`: ‘унктор дл€ сравнени€ строк в контейнере `std::map`.
- `TextMapTest()`: ƒемонстрирует использование линейного аллокатора в `std::map` дл€ подсчета вхождений слов.
- `GetCurrentTimeMs()`: ѕолучает текущее врем€ в миллисекундах.

### ќсновна€ логика:

1. —оздаетс€ линейный аллокатор дл€ целых чисел.
2. ¬ыполн€ютс€ операции выделени€ и освобождени€ пам€ти с использованием линейного аллокатора.
3. «атем вызываетс€ функци€ `TextMapTest()`, использующа€ линейный аллокатор дл€ `std::map`.
4. »змер€етс€ врем€ выполнени€ `TextMapTest()`.

### ѕримечание:

Ётот код предназначен дл€ демонстрации использовани€ линейного аллокатора и может быть расширен или использован в более сложных программах.